services:
  alpacasay:
    build: .
    container_name: alpacasay
    volumes:
      - .:/app
    environment:
      - TERM=xterm-256color
    command: ["--alpaca", "happy", "--color", "green", "Hello from Docker! 🐳"]

